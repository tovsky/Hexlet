
	Квантификатор это спец ограничитель, который указывает количество возможных
	 повторений у группы, символов или классов символов 
	 
			colou?r 			// ? ноль или одно совпадение (color colour)
			col(ou)?r
			col[ou]?r
			
			colou+r				// + одно и более совпадений  (colour colouur colouuuuur )
			
			colou*r				// * ноль или более совпадений (color colour colouur colouuuuur )
			
			colou{2}r			// точное число повторений 		(colouur )
			
			colou{2,3}r			// Кол-во повторений от 2 до 3
			
			colou{1,}r			// мин количество повторений 1 раз и более
			
			
			
ТЕСТЫ
	Квантификатор это
		ограничитель повторения
		
	Можно ли использовать квантификатор для указания точного числа повторений?
		да, для этого используются фигурные скобки { }
		
	Дан текст:
		sample text is sooo good for testing
	И регулярное выражение:
		/o{2,}/g
	Сколько совпадений будет найдено?
				2	
				
ЗАДАНИЕ
	Напишите регулярное выражение, которое находит email адреса удовлетворяющие следующим условиям:
	Часть до @ может содержать не менее одного символа класса \w. Пример info;
	Часть после @ и до . после которой начинается домен, может содержать только буквы и быть не короче трех символов. Пример hexlet;
	Часть после . может содержать только буквы и быть от 2 до 5 символов в длину. Пример io.
	
					BEGIN
					^\w+@[a-zA-Z]{3,}\.[a-zA-Z]{2,5}$
					END