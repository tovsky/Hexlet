
	Цель: Рассмотреть концепцию присваивания и изменения состояния.
	
	Объект обалдает состоянием, если на него влияет его истори. (например банковский счет)
	
	
	
	
	
ССЫЛКИ
	http://php.net/manual/en/language.operators.assignment.php
	http://php.net/manual/ru/language.operators.assignment.php
	
	
	
ТЕСТЫ
	Создана переменная X, содержащая численное значение. Возможно ли такое, что во время первого 
	 запуска программы значение этой переменной не изменилось, а во время второго запуска программы 
	 значение изменилось (при этом не на другое число, а на строку, то есть набор символов)?
		Да
		
	Может ли поведение функции зависеть от значения переменных, которые находятся снаружи функции?
		Да
		
	После добавления возможности сохранения состояния в нашем коде появилась такая особенность: вызов 
	 функции с одинаковыми аргументами может приводить к разным результатам. Является ли эта 
	 особенность плюсом?
			Нет, это усложняет проверку корректности кода
			


ЗАДАНИЕ
	Solution.php
	Напишите функцию newWithdraw, которая снимает деньги со счета. При попытке снять 
	 больше денег, чем есть на счете, она должна возвращать too much.

	Пример:

	$withdraw = newWithdraw(100);
	$withdraw(1000); // 'too much'
	$withdraw(50); // 50
	$withdraw(45); // 5
	
								// Решение
									<?php

									namespace App\Solution;

									// BEGIN (write your solution here)

									function newWithdraw($balance)
									{
										return function ($amount) use (&$balance) {
											if ($balance >= $amount) {
												$balance -= $amount;
												return $balance;
											} else {
												return "too much";
											}
										};
									}
									// END


