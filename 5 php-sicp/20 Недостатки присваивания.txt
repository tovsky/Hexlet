
	Цель: Рассмотреть проблемы, связанные с использованием присваивания и сохранения состояния.
	
	Функциональное программирование - то в котором не используется присваивания. Это программирование с 
	 помощью занчений.
	 
	Чистая функция - ее выход зависит только от ее входа.
	
	Ссылочная или реферициальная прозрачность.
	
ТЕСТЫ
	Существует функция, которая получает на вход целое число и возвращает произведение этого 
	 числа и текущей даты. Например, если сегодня 29-е число, то вызов с аргументом 11 
	 вернет 319 (29х11). Является ли такая функция "чистой"?
			нет
			
	Можно ли в одной программе одновременно использовать и функциональные и императивные подходы?
		да, конечно
		
	Каким одним словом можно описать сложности и проблемы императивного программирования?
		состояние
		
		
		
ЗАДАНИЕ
	Solution.php
	Реализуйте функцию fib находящую числа Фибоначчи используя рекурсивно-итеративный процесс, 
	 но вместо аккумулятора параметров для вложенной функции $iter используйте переменные.

	Формула:

	f(0) = 0
	f(1) = 1
	f(n) = f(n-1) + f(n-2)
	Пример:

	2 == fib(3);
	5 == fib(5);
	55 == fib(10);
	
					// Решение
						<?php

						namespace App\Solution;

						function fib($num)
						{
							// BEGIN (write your solution here)
							$tmp;
							$fib1 = 0;
							$fib2 = 1;
							$i = 0;

							$iter = function () use ($num, &$iter, &$fib1, &$fib2, &$i) {
								if ($i < $num) {
									$tmp = $fib1;
									$fib1 = $fib2;
									$fib2 = $tmp + $fib2;
									$i += 1;
									$iter();
								}
							};

							$iter();
							return $fib1;
							// END
						}