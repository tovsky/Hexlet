
	Side effects.  побочные эффекты.
		Инструкции имеют смысл только в том случае, если они производят side effects, 
		 т.е. например делают вывод на экран, отправку по сети, запись в базу...
		Иначе процессор напрягается а полученный результат не используется.
		
	Переменные. 
		Можно сохранять промежуточные значения, чтобы использовать далее в программе.
		
		
				function average($num1, $num2, $num3)
				{
					$amount = $num1 + $num2 + $num3;
					
					return $amount / 3;
				}
				
	Удаление переменной.
				unset $x; 
				
				
				
ЗАДАНИЕ
	Реализуйте функцию solution, которая принимает на вход число и возвращает результат 
	 следующих действий:
	Число возводится во 2 степень.
	Результат возводится в -2 степень.
	Результат возводится, снова, во 2 степень.
	Используйте переменную $result для накопления результата вычисления.
	
					function solution($start)
					{
					// BEGIN (write your solution here) (write your solution here)
						$result = $start ** 2;
						$result = $result ** (-2);
						$result = $result ** 2;
						
						return $result;
						
					// END
					}
					
					
					
					
					