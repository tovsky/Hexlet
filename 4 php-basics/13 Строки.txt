
	. 		// операция конкатенации (объединении нескольких строк в одну).
	
	' \' '  		// Экранирование. 
	
	escape sequence  эскейп последовательности. 
	Последовательности символов, влияющих на вывод в терминал.
	escape sequence   интерпретируются в "". а в '' нет.
		\t 				// табуляция
		\n				// перевод строки
		\\				//  вывод \
		\\n				//  вывод \n
		
	. PHP_EOL		// константа end of line  добавится в конце строки и будет интерпертровано в зависимости от ОС.
	
	
	Интерполяция значения
		"$value "		// подставится значение переменной. только для "".
	
	
	hereDoc  			// можем писать все переменные и они будут интерпертроваться.
	<<<EOD 
			.......
EOD;


	nowDoc
	<<<'EOD'			// Все спец символы без интерполяции
			.......
EOD;
		
		
	Обращение к символу в строке.
			$str = 'simple string';
			echo $str[0];
			echo $str[7];
			echo $str[strlen($str) - 1];			// Вывод последнего символа
			
			
			
			
			
ЗАДАНИЕ
	Реализуйте функцию reverse, которая переворачивает строку.
	Пример:
	reverse('hello, world!'); // !dlrow ,olleh
	Подсказки
	Для подсчета длины строки используйте функцию strlen.
			
			function reverse($str)
			{
				$result = '';

				for ($i = strlen($str) - 1; $i >= 0; $i--) {
					$result .= $str[$i];
				}

				return $result;
			}
		
		
		
		
		
		
		
		
		