
	
		cat			 		// Вывод содержимого всего файла
		
		head wifi.log		// Вывод первых 10 строк
		tail				// Вывод последних 10 строк
		
		tail -f system.log	// Например в файл system.log  идет добавление строк,
							// данный ключ -f  продолжит обновлять содержимое файла.
							// Часто используют при просмотре логов серверов.
							(выход из этого состояния Ctrl + C)
							
							
		grep 'Apr 27' system.log	// Поиск подстроки в файле
		
		
		